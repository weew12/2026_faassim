# skippy 包结构与导航

> 总览文档。读完这份就掌握了 skippy 包的骨架；想深入任何模块，请跳转到对应子文档。

## 1. 包的目的

`skippy/` 是本版本内置的**调度器子包**，用于替换原先的 `edgerun-skippy-core` 外部依赖。它提供 Kubernetes 风格的调度模型：

- 把**函数副本**抽象为 `Pod`
- 把**边缘/云节点**抽象为 `Node`
- 通过「**谓词过滤 + 优先级打分**」的流程选择部署节点

在 faas-sim 业务流程中，Skippy 负责完成「**函数副本 Pod 应该放到哪个节点**」这一核心决策：

| 输入 | 输出 |
| --- | --- |
| faas-sim 适配出的 Pod、节点资源、镜像状态、带宽图、对象存储位置 | 建议节点、可行节点数量、该节点还需要拉取的镜像列表 |

## 2. 目录结构

```text
skippy/
├── __init__.py
└── core/
    ├── __init__.py
    ├── model.py
    ├── clustercontext.py
    ├── predicates.py
    ├── priorities.py
    ├── scheduler.py
    ├── storage.py
    └── utils.py
```

## 3. 文件职责一览

| 文件 | 职责 | 子文档 |
| --- | --- | --- |
| `__init__.py` | 包级元信息，保留外部代码可能依赖的 `name = 'skippy'` 变量 | [01](01_包入口与core子包.md) |
| `core/__init__.py` | core 子包元信息，给出子包结构总览 | [01](01_包入口与core子包.md) |
| `core/model.py` | 定义调度领域对象：`ImageState` / `ResourceRequirements` / `Container` / `PodSpec` / `Pod` / `Capacity` / `Node` / `SchedulingResult` | [02](02_调度领域模型_model.md) |
| `core/clustercontext.py` | 集群运行态上下文：节点、镜像、剩余资源、带宽图、对象存储索引；提供 `place_pod_on_node` / `remove_pod_from_node` 状态更新接口 | [03](03_集群运行态上下文_clustercontext.md) |
| `core/predicates.py` | 调度**过滤**逻辑：资源充足性、节点标签存在/不存在性 | [04](04_调度谓词过滤_predicates.md) |
| `core/priorities.py` | 调度**打分**逻辑：资源均衡、镜像本地性、带宽感知镜像拉取代价、数据本地性、边缘节点优先、硬件能力匹配 | [05](05_调度优先级打分_priorities.md) |
| `core/scheduler.py` | 串联过滤和打分流程，选择最高分节点，并将调度结果写回 `ClusterContext` | [06](06_调度器主流程_scheduler.md) |
| `core/storage.py` | 对象存储 bucket / 数据对象 / 存储节点之间的索引，供数据本地性调度使用 | [07](07_对象存储索引_storage.md) |
| `core/utils.py` | 镜像名规范化、容量字符串解析、计时器、递增计数器等基础工具 | [08](08_调度工具函数_utils.md) |

## 4. 模块依赖关系

```text
                ┌────────────────────────────────────────┐
                │              scheduler.py              │
                │  (filter → map_score → reduce → pick)  │
                └───────┬─────────────────┬──────────────┘
                        │                 │
              ┌─────────▼─────┐   ┌───────▼────────┐
              │ predicates.py │   │ priorities.py  │
              └───────┬───────┘   └───────┬────────┘
                      │                   │
                      └────────┬──────────┘
                               │
                ┌──────────────▼──────────────┐
                │       clustercontext.py     │
                │  (节点/镜像/带宽/存储索引)  │
                └──────┬──────────────┬───────┘
                       │              │
                ┌──────▼──────┐  ┌────▼─────┐
                │  model.py   │  │storage.py│
                └─────────────┘  └──────────┘
                       ▲              ▲
                       │              │
                       └──────┬───────┘
                              │
                       ┌──────▼──────┐
                       │  utils.py   │
                       └─────────────┘
```

依赖方向：**调度器 → 谓词/优先级 → 集群上下文 → 领域模型/存储索引 → 工具函数**。下层不引用上层。

## 5. 核心业务流程

1. faas-sim 通过 `sim/skippy.py` 将 Ether 节点转换为 Skippy `Node`，将函数部署转换为 Skippy `Pod`。
2. `Scheduler.schedule(pod)` 从 `ClusterContext` 获取节点列表。
3. 调度器执行**默认谓词**：资源必须足够，且普通函数不能被放到存储专用节点。
4. 对可行节点执行**默认优先级函数**：资源均衡、镜像拉取代价、边缘位置、数据本地性、能力匹配。
5. 调度器选择总分最高的节点，并计算该节点**缺失的镜像列表**。
6. 调度器调用 `ClusterContext.place_pod_on_node()`，扣减节点剩余资源并登记镜像缓存状态。
7. faas-sim 根据调度结果继续模拟镜像拉取、函数启动、setup 和请求执行。

## 6. 子文档索引

- [01_包入口与core子包.md](01_包入口与core子包.md) — `skippy/__init__.py`、`core/__init__.py`
- [02_调度领域模型_model.md](02_调度领域模型_model.md) — `core/model.py`
- [03_集群运行态上下文_clustercontext.md](03_集群运行态上下文_clustercontext.md) — `core/clustercontext.py`
- [04_调度谓词过滤_predicates.md](04_调度谓词过滤_predicates.md) — `core/predicates.py`
- [05_调度优先级打分_priorities.md](05_调度优先级打分_priorities.md) — `core/priorities.py`
- [06_调度器主流程_scheduler.md](06_调度器主流程_scheduler.md) — `core/scheduler.py`
- [07_对象存储索引_storage.md](07_对象存储索引_storage.md) — `core/storage.py`
- [08_调度工具函数_utils.md](08_调度工具函数_utils.md) — `core/utils.py`
