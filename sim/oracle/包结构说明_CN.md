# `sim/oracle` 包结构说明

估计器包：为执行时间、启动时间、资源占用、成本和经验分布采样提供统一接口。

## 包内 Python 文件

- `__init__.py`：该文件参与本包对应的仿真支撑逻辑。
- `oracle.py`：性能与资源 Oracle 抽象集合，封装启动时间、执行时间、带宽、成本、资源利用率和拟合分布采样等估计接口。

## 子目录

- `data/`：估计器数据工具包：封装经验分布等数据采样工具。
